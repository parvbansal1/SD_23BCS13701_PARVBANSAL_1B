spring.application.name=StayNexus

#spring.datasource.url=jdbc:mysql://localhost:3306/airbnb
#spring.datasource.username=root
#spring.datasource.password=Parvbansal@1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Server Configuration
server.servlet.context-path=/api/v1
server.port=8080

server.forward-headers-strategy=framework
server.use-forward-headers=true

# JWT Secret
jwt.secretKey=mySuperSecretJwtKeyForAirbnbProject123456

# Frontend URL
frontend.url=http://localhost:3000

# Stripe Configuration (dummy for now)
stripe.secret.key=sk_test_dummy
stripe.webhook.secret=whsec_dummy

# Swagger
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
